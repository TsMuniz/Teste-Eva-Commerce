import styled from 'styled-components'

const MainPageTemplateWrapper = styled.body`
  .foo-bar {
    font-family: 'Roboto', sans-serif;
    font-size: 1.5rem;
  }
`

export default MainPageTemplateWrapper